
* Exercise 1: Calculate the Sum of Two Numbers

  // Begin
  // Print "Input two numbers to add"
    // Input num1 
    // Input num2
    // Num3 == num1 + num2
 // Print "Sum = num3"

* Exercise 2: Check if a Number is Even or Odd
 
 // num3 is even if divisible by 2
   // True == num3 / 2
   // False != num3  / 2
 // Print "Even" next to num 3 if true or PRINT "Odd" if false 
 

* Exercise 3:
 
Write pseudo code that will perform the following.    

a) Read in 5 separate numbers.   
b) Calculate the average of the five numbers.
c) Find the smallest (minimum) and largest (maximum) of the five entered numbers.
d) Write out the results found from steps b and c with a message describing what they are

 // Print "Input 5 numbers one at a time"
    // Input num1 (55)
    // Input num2 (43)
    // Input num3 (62)
    // Input num4 (21)
    // Input num5 (99)
 // Show Average of the 5 numbers
  // Average == num1 + num2 + num3 + num4 + num5 / 5
 // Print "Average is 56" 
 // Find highest number of num1 through num5 
  // Smallest == lowest value of num1 or num2 or num3 or num or num5
  // Largest == Highest value of num1 or num2 or num3 or num4 or num5 
 //  Print "99 is largest" and "21 is the smallest" 

-- END --
